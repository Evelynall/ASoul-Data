# 2026-04-09

## 新增手动修改日程工作流

- 新建 `scripts/patch-schedule.js`：读取 `PATCH_DATA` 环境变量（JSON），按 id 定位日程并修改允许的字段，字段有白名单保护，支持单条和批量修改，修改后自动更新 version 和 lastUpdate。
- 新建 `.github/workflows/patch-schedule.yml`：workflow_dispatch 触发，接收 `patch_data` 文本输入，先做 JSON 格式校验，再执行脚本，最后提交推送。
- 更新 `README.md`，新增"手动修改日程数据"使用文档，包含字段表格和 API 远程调用示例。
